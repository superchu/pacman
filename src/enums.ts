export enum GameState {
  Playing,
  Paused,
  GameOver
}

export enum Key {
  Up = 38,
  Down = 40,
  Left = 37,
  Right = 39,
  Space = 32,
  P = 80,
}

export enum Direction {
  Left,
  Right,
  Up,
  Down
}